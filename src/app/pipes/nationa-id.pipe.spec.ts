import { NationaIDPipe } from './nationa-id.pipe';

describe('NationaIDPipe', () => {
  it('create an instance', () => {
    const pipe = new NationaIDPipe();
    expect(pipe).toBeTruthy();
  });
});
